SecurePass - Password Generator 🔐

SecurePass is a desktop application designed to generate strong, cryptographically secure passwords. Built with Python's secrets module, it ensures that every generated password is unpredictable and suitable for high-security environments.

✨ Key Features

🛡️ Cryptographically Secure: Uses the Python secrets library (instead of the standard random module) for true unpredictability.

🎛️ Customizable Complexity:

Adjustable password length (Minimum 8 characters).

Toggle options for Uppercase, Lowercase, Numbers, and Symbols.

🚫 Exclusion Filter: Specific field to exclude ambiguous characters (e.g., l, 1, O, 0) or characters not allowed by certain websites.

📋 One-Click Copy: Instantly copy the generated password to your clipboard.

💻 Clean GUI: Simple, user-friendly interface built with Tkinter.

📱 Usage Guide

Set Length: Enter your desired password length (e.g., 16).

Select Options: Check the boxes for the character types you want to include.

Exclude (Optional): Type any characters you don't want to appear in the "Exclude Characters" box.

Generate: Click the "Generate Password" button.

Copy: Click "Copy to Clipboard" to use your new password immediately.
